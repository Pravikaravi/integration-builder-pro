{
  "buildCommand": "npx vite build --config vite.config.spa.ts",
  "outputDirectory": "dist-spa",
  "framework": null,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
